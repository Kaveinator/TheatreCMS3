@using TheatreCMS3.Models
@model LoginViewModel

@*No .css file is loaded because this partial view will only be loaded in rentalhistories area.*@
@if (!Request.IsAuthenticated)
{
    using (Html.BeginForm("Login", "Account", new { area = "" }))
    {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(m => m.EmailOrUsername, new { @Value = "HistoryManager" })
        @Html.HiddenFor(m => m.Password, new { @Value = "H1stORYM@n@g3r" })
        @Html.HiddenFor(m => m.RememberMe, new { @Value = false })
        <input type="submit" value="Log in as History Manager" class="btn history-manager" />
    }
}
